/*
Config object to use for connecting to the desired beam account.
You can retrieve all the ids when calling https://beam.pro/api/v1/users/current from the browser
*/
module.exports = {
    user: 'user'; //username of the beam account
    password: 'password'; //password of the beam account
    channel: 'channel'; //channel name of the beam account
};