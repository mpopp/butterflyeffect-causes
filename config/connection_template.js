/*
copy that file and rename the copy to "connection.js". Use the copy to add you own credentials.
*/
module.exports = {
    user: 'user', //username of the beam account
    password: 'password', //password of the beam account
    channel: 'channel' //channel name of the beam account
};